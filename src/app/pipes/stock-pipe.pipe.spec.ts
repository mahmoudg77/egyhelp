import { StockPipePipe } from './stock-pipe.pipe';

describe('StockPipePipe', () => {
  it('create an instance', () => {
    const pipe = new StockPipePipe();
    expect(pipe).toBeTruthy();
  });
});
